# SQLite — chỉ tham chiếu

Thư mục này lưu **schema và migration SQLite cũ** để đối chiếu khi cần.

- **Không chạy** `prisma migrate` với `schema.sqlite.prisma` trong luồng dev/prod hiện tại.
- DB đang dùng: **PostgreSQL** (`schema.prisma` + `prisma/migrations/`).
